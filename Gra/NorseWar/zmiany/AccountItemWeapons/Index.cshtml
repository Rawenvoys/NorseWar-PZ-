@model IEnumerable<NorseWar.Models.AccountItemWeapon>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Account.Login)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemWeapon.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Equiped)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Account.Login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ItemWeapon.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Equiped)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccountItemWeaponID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccountItemWeaponID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccountItemWeaponID })
        </td>
    </tr>
}

</table>
