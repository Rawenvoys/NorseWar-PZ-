@model IEnumerable<NorseWar.Models.AccountFriend>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Account.Login)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Friend.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Account.Login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Friend.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccountFriendID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccountFriendID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccountFriendID })
        </td>
    </tr>
}

</table>
